(jbuild_version 1)

(executable
 ((name projetM2)
  (public_name projetM2) 	
  (libraries (bistro.bioinfo bistro.utils bistro_server core))
  (preprocess (pps (ppx_sexp_conv bistro_server.ppx ppx_let)))
))

